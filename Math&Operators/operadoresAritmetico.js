const [a, b, c, d] = [9, 2, 8, 10]

const soma = a + b + c + d;
const subtracao = d - a;
const multiplicacao = a * c;
const divisao = d / b;
const modulo = c % 2;

console.log(soma, subtracao, divisao, multiplicacao, modulo);